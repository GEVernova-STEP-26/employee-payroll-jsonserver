{
  "name": "employee-payroll-app",
  "version": "1.0.0",
  "description": "A simple CRUD application for managing employee payroll",
  "main": "index.html",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "server": "json-server --watch db.json --port 3000 --delay 500"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
